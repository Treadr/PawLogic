# Database Configuration
# For Docker Compose: use "db" as host; for local dev: use "localhost"
DATABASE_URL=postgresql+asyncpg://PawLogic_DB:PPaaPA55!!word@db:5432/PawLogic

# Supabase (use JWT secret for local dev token signing)
SUPABASE_URL=http://localhost:54321
SUPABASE_JWT_SECRET=pawlogic-dev-jwt-secret-key-minimum-32-chars
SUPABASE_SERVICE_KEY=

# Anthropic Claude API
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Redis
# For Docker Compose: use "redis" as host; for local dev: use "localhost"
REDIS_URL=redis://redis:6379/0

# Application
ENVIRONMENT=development
LOG_LEVEL=DEBUG
CORS_ORIGINS=["http://localhost:8081","http://localhost:19006"]
